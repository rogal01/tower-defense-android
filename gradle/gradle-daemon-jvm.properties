# Public-clone friendly daemon requirement: Java 21 is needed by the current
# Gradle/AGP toolchain, but the vendor is left open so local Android Studio JBR
# or another compatible JDK can satisfy it.
toolchainVersion=21
